/**
 * Shared mobile input state — written by VirtualJoystick, read by CameraController.
 * Plain object (not reactive) for zero-overhead 60fps reads.
 */
export const mobileInput = { x: 0, y: 0, active: false };
