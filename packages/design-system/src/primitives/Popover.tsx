/* Distinct from `Menu`, whose popup is a `menu` of `menuitem`s: a popover holds
   controls, which keep their own roles and keyboard behaviour inside it. */
export { Popover } from "@base-ui/react/popover";
